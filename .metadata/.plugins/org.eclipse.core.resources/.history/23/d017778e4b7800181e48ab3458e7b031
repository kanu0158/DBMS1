-- account
-- money(decimal),  account_seq(decial), account_num, 
-- account_type, create_date, limit
CREATE TABLE account(
	account_num VARCHAR2(20),
	account_type VARCHAR2(20),
	create_date DATE,
	money DECIMAL,
	account_seq DECIMAL PRIMARY KEY,
	limit DECIMAL
);
DROP TABLE account;

INSERT INTO account VALUES(
	'123-456-789', '일반통장', SYSDATE, 0, 1, 0
);

INSERT INTO account(
 account_num, account_type, create_date, money, account_seq, limit
) 
VALUES(
	'125-222-222', '일반통장', SYSDATE, 1000, 2, 0
);

SELECT * FROM account;
SELECT * FROM TAB;

